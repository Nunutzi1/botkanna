const Discord = require('discord.js');

module.exports = {
    name: "alex",
    alias: [],

    execute (client, message, args){
        message.channel.send(`Ese we es el novio del giovanni`);
    }
}